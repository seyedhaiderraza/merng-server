{
    "version": 2,
    "builds": [{
        "src": "server.js",
        "use": "@vercel/node"
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "server.js"
    }],
    "env": {
        "MONGO_DB": "@MONGO_DB",
        "PORT": "@PORT",
        "SECRET_KEY": "@SECRET_KEY"
    }
}